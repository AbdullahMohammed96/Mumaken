@{
    Layout = "_Layout";
}
<div class="row justify-content-center">
    <div class="col-lg-9">
        <h2 class="fs15 ff-d mb-4 text-center"> @localizer["About App"]</h2>

        <p class="fs13 lh-md c-gray text-center">
            @Html.Raw(ViewBag.aboutUs)
        @*     @if (ViewBag.lang == "ar")
            {
                <div style="font-family: 'Arial', sans-serif; white-space: pre-wrap; direction: rtl;">
                    @Html.Raw(ViewBag.aboutUs)
                </div>
            }
            else
            {
                <pre>
                    @Html.Raw(ViewBag.aboutUs)
                </pre>
            } *@
        </p>
    </div>
</div>